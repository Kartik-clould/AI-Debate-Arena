# AI Debate Arena

**AI Debate Arena** is an AI-powered debate system that simulates a discussion between two AI agents with opposite viewpoints. The system generates arguments, performs rebuttals, evaluates the debate using an AI Judge, and uses an Artificial Neural Network (ANN) to predict the final winner.

## Features

* Pro AI Agent argues in favour of the topic
* Con AI Agent argues against the topic
* AI-generated rebuttal rounds
* AI Judge evaluates both sides
* Six evaluation scores:

  * Quality
  * Relevance
  * Reasoning
* ANN-based winner prediction
* Pro and Con winning probabilities
* Interactive React-based interface

## How It Works

```text
User enters a topic
        ↓
Pro Agent + Con Agent
        ↓
Rebuttal Rounds
        ↓
AI Judge
        ↓
Six Evaluation Scores
        ↓
ANN Model
        ↓
Winner + Probabilities
```

The six scores generated by the AI Judge are provided as input to the ANN model. The ANN then predicts whether the **Pro** or **Con** side wins and provides the probability for each side.

## Technologies Used

### Backend

* **Python** – Main programming language
* **FastAPI** – Backend API
* **Groq API** – AI/LLM integration
* **python-dotenv** – Environment variable management

### Machine Learning

* **Scikit-learn** – Machine learning library
* **MLPClassifier** – Artificial Neural Network
* **Pandas** – Dataset handling and processing
* **CSV** – ANN training dataset

### Frontend

* **React** – User interface
* **Vite** – Frontend development and build tool
* **JavaScript, HTML, CSS** – Frontend functionality and styling

## Project Structure

```text
AI-Debate-Arena/
├── agents.py
├── ann_model.py
├── ann_dataset.py
├── debate_dataset.csv
├── debate_dataset_v2.csv
├── generate_dataset.py
├── main.py
├── models.py
├── requirements.txt
│
├── frontend/
│
└── frontend-react/
    ├── src/
    ├── public/
    ├── package.json
    └── vite.config.js
```

## Running Locally

### Backend

Install the required packages:

```bash
pip install -r requirements.txt
```

Create a `.env` file:

```text
GROQ_API_KEY=your_api_key
```

Start FastAPI:

```bash
uvicorn main:app --reload
```

Backend:

```text
http://127.0.0.1:8000
```

### Frontend

```bash
cd frontend-react
npm install
npm run dev
```

Frontend:

```text
http://localhost:5173
```

## Purpose

The project demonstrates the integration of **Large Language Models, multi-agent AI, Natural Language Processing, and Artificial Neural Networks** into a single application.
